return {
  "sphamba/smear-cursor.nvim",
  opts = {
    smear_insert_mode = true,

    -- Smooth cursor without smear (https://github.com/sphamba/smear-cursor.nvim)
    stiffness = 0.5,
    trailing_stiffness = 0.5,
    matrix_pixel_threshold = 0.5,
    ---
  },
}
